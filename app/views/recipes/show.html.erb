<turbo-frame id="selected-recipe-<%= @recipe.id %>">
  <div class='flex mr-6 mt-6 max-w-lg w-full bg-white mx-auto min-w-300'>
    <div class="relative transform hover:-translate-y-2 transition ease-in-out duration-500 text-white">
      <div class="absolute rounded inset-0 z-10 transition duration-300 ease-in-out bg-gradient-to-t from-black via-gray-900 to-transparent"></div>
      <div class="relative cursor-pointer group z-10 px-10 pt-10 space-y-6">
        <div class="align-self-end w-full">
          <div class="h-32"></div>
          <div class="space-y-6 detail_info">
            <div class="flex flex-col space-y-2 inner">
              <h3 class="text-2xl font-bold text-white" data-unsp-sanitized="clean"><%= @recipe.title %></h3>
              <div class="mb-0 text-lg text-gray-400"><i><%= @recipe.fields[:chef_name] %></i></div>
            </div>
            <div class="flex flex-row justify-between datos">
              <% (@recipe.fields[:tags] || []).each do |tag| %>
                <div class="flex flex-col datos_col">
                  <div class="text-sm text-gray-400"><%= tag.name %></div>
                </div>
              <% end %>
            </div>
            <div class="flex flex-col overview">
              <div class="flex flex-col"></div>
                <p class="text-xs text-gray-100 mb-6">
                  <%= markdown(@recipe.fields[:description]) %>
                </p>
              </div>
            </div>
          </div>
       </div>
      <img class="absolute rounded inset-0 transform w-full" src="<%= @recipe.photo&.url %>" style="filter: grayscale(0);" />
      <div class="flex flex-row relative pb-10 space-x-4 z-10 mt-3">
        <a class="flex items-center py-2 px-4 rounded-full mx-auto text-white bg-red-500 hover:bg-red-700" href="<%= root_path %>">
          <div class="text-sm text-white ml-2 mr-2">Hide</div>
        </a>
      </div>
    </div>
  </div>
</turbo-frame>
